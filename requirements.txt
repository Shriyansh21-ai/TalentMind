# TalentMind — runtime dependencies.
# Deliberate, top-level packages only; incidental transitive plumbing and
# test/dev tooling are omitted (the latter live in requirements-dev.txt).
pandas
numpy
pydantic
scikit-learn
sentence-transformers
torch
streamlit
plotly
faiss-cpu
