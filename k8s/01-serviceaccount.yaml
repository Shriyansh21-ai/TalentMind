# Dedicated, least-privilege service account for the app pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: talentmind
  namespace: talentmind
  labels:
    app.kubernetes.io/name: talentmind
automountServiceAccountToken: false
