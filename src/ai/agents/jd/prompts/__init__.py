"""Versioned prompt templates for the JDAnalystAgent.

Loaded by the agent's own :class:`~src.ai.prompts.loader.PromptLoader` (pointed at
this directory) so the agent's prompts live with the agent — additive, and does
not touch the shared prompt library used by other agents.
"""
